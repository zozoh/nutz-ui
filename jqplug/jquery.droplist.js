/*
 * 绘制一个droplist，提供翻页，过滤功能
 */
(function($) {
    $.fn.extend({
        droplist: function(opt) {
            var sel = this;

            // 检查opt文件的配置项是否完整

            

            return sel;
        }
    });
})(window.jQuery);